<tr>
	<td></td>
	<td><%= link_to category.name.capitalize, category_path(category.id) %></td>
	<td><%= link_to 'Edit', edit_category_path(category.id), class: "btn btn-info btn-xs" %></td>
	<td><%= link_to 'Delete', category_path(category.id), method: :delete,class: "btn btn-danger btn-xs" %></td>
</tr>